(async () => {
  const productImageElements = document.querySelectorAll("#productImage");
  const productNameElements = document.querySelectorAll("#productName");
  const productColorElements = document.querySelectorAll("#productColor");
  const productPriceElements = document.querySelectorAll("#productPrice");

  console.log(productColorElements);
})();
